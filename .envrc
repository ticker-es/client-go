source_up

export PROJECT_ROOT=$(pwd)
export PROJECT_NAME=$(basename $PROJECT_ROOT)

export TICKER_CA_CERT="../certs/ca.crt"
export TICKER_CLIENT_CERT="../certs/client-1.crt"
export TICKER_CLIENT_KEY="../certs/client-1.key"
